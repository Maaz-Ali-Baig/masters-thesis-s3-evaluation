# Benchmark result raw data (large files)
*.csv.bak
*.log

# Docker volumes and temp data
data/
volumes/

# Environment and secrets
.env
*.env
config/secrets*
# Real S3 credentials for the testbed. These live in ~/.thesis-s3-env (outside
# the repo) and are read by scripts/s3-helpers.sh. This entry is defence in
# depth in case a copy is ever placed inside the repository by accident.
.thesis-s3-env
*.thesis-s3-env

# OS files
.DS_Store
Thumbs.db

# Python cache
__pycache__/
*.pyc

# VS Code settings
.vscode/

# AWS CLI v2 installer (downloaded zip + unpacked install, not thesis content)
awscliv2.zip
aws/

# Internal planning docs, not for the tracked repo history
Context/

