# RustFS

No separate config file yet — the root user/password are passed directly as environment
variables in the `docker run` command (see `setup_notes.md` at the repo root). If RustFS
testing later needs a config file (e.g. for TLS or access policies), it goes here.
